以下规则适用于本仓库所有协作与代码修改任务：

@CLAUDE.md

## 默认上下文
- 开始任何任务前，先阅读并遵守 `@CLAUDE.md`。

## Git / 提交规范
- **所有 git commit message 必须使用中文**（简洁、明确，优先写“为什么/目的”，再写“做了什么”），句末不要有句号。
- commit message 建议格式：
  - `fix：...`
  - `feat：...`
  - `refactor：...`
  - `docs：...`
  - `exp：...`
